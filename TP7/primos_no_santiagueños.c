#include <stdio.h>

int main() {
    int ene, num, counter;
    printf("Ingrese el numero N: ");
    scanf("%i", &ene);
    
    counter = 0;

    while (counter < ene)
    {
        counter += 1;
        if (counter%counter == 0 && ) {
            printf("%i\n", counter);
        }
    }
    

}